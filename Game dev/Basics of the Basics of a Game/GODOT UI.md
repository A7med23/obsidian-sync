# start with canvas layer
## placed on top of everything and has it's own rendering layer

## contains

### control node -> HUD
- is it's own scene
- has score, hp, $, etc etc

- can use H container or V container to stack textures horizontally/vertically 